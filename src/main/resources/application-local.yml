spring:
  config:
    activate:
      on-profile: local

  liquibase:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/distance-calculator
    username: root
    password: root
    name: distance-calculator

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: update